"""Import-only stand-in for ``models.warmup_profile`` (test machines only)."""


def load_warmup_profile(*args, **kwargs):
    raise NotImplementedError(
        "models stub: load_warmup_profile() is not available in tests"
    )
